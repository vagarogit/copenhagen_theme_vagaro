<a class="skip-navigation" tabindex="1" href="#main-content" onclick="
  // Fallback for skip navigation if main-content doesn't exist
  const target = document.getElementById('main-content');
  if (!target) {
    const fallbacks = ['.container', '.section', 'main', 'article'];
    for (const selector of fallbacks) {
      const fallback = document.querySelector(selector);
      if (fallback) {
        fallback.focus();
        fallback.scrollIntoView();
        return false;
      }
    }
  }
  return true;
">{{t "skip_navigation"}}</a>

<div class="relative h-16 max-h-16 min-h-[64px] bg-primary transition delay-150 ease-in-out">
        <a
          href="https://www.vagaro.com/pro/pos-hardware"
          class="mx-auto flex h-full w-full flex-none cursor-pointer flex-row items-center justify-center gap-6 border-none py-4 text-center text-base leading-8 transition-colors sm:border-none xl:border-none"
          id="paypro-banner"
          rel="noopener noreferrer"
          target="_blank"
          style="background: linear-gradient(90deg, #422929 0%, #CC4744 64.07%, #773838 100%);"
        >
          <div class="relative flex items-center">
            <div class="mx-auto mr-0 flex h-16 items-center justify-start">
              <img
                src="https://us-west-2.graphassets.com/AalLHDRueT6SDLkGLppQVz/X993Y4sUTNC5Rh2x2XMa"
                alt="PayPro promotional banner"
                class="block h-full w-auto max-w-none border-0 object-contain"
                width="128"
                height="110"
              />
            </div>
            <div class="flex flex-col justify-center gap-1 md:flex-row md:justify-start md:gap-0">
              <div class="mx-5">
                <div class="cursor-pointer leading-5 text-white md:inline-block">
                  <p>Meet PayPro, Your New Front Desk.</p>
                </div>
              </div>
              <div class="mx-auto flex max-w-[110px] items-center justify-center rounded-full bg-charcoal px-3.5 py-[3px] text-xs font-bold text-white hover:bg-charcoal hover:text-white md:ml-0 md:py-1">
                LEARN MORE
              </div>
            </div>
          </div>
        </a>
  </div>
<header class="bg-white sticky top-0 z-50 shadow-sm">
  <div class="mx-auto">
    <!-- Top row with logo and auth buttons -->
    <div
      class="relative flex h-16 justify-center items-center border-b border-gray-100 mx-auto px-4 border w-full"
    >
      <!-- Logo -->
      <div class="absolute left-2 md:left-4 lg:left-8 flex shrink-0 items-center">
        {{#link "help_center" class="flex items-center"}}
          <img
            src="{{asset 'logo.svg'}}"
            alt="{{t 'home_page' name=help_center.name}}"
            class="h-8 w-auto"
          />
        {{/link}}
      </div>

      <!-- Mobile Sign Up Button (centered) -->
      <a
        href="https://www.vagaro.com/pro/pricing"
        class="lg:hidden flex items-center justify-center bg-primary hover:bg-charcoal hover:border-solid text-md text-white font-medium py-2 px-8 md:px-4 rounded-full w-auto md:w-[152px] h-[40px]"
      >
        <span class="text-white font-semibold">Sign Up</span>
      </a>

      <!-- Auth & Region -->
      <div class="absolute right-4 lg:right-8 flex items-center space-x-4">
        <button
          type="button"
          class="text-gray-600 hover:text-gray-900 lg:hidden hidden"
        >
          <svg
            class="w-5 h-5"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"
            ></path>
          </svg>
        </button>

        <div class="hidden lg:flex items-center space-x-4">
          <div class="flex items-center mr-4">
            <img
              src="https://flagicons.lipis.dev/flags/4x3/us.svg"
              alt="US"
              class="w-5 h-auto mr-1"
            />
            <span class="text-sm font-medium">US</span>
          </div>

          {{!-- <a
            href="#"
            class="text-sm font-medium text-charcoal hover:text-gray-900 mr-4"
          >Log In</a> --}}

          <a
            href="https://www.vagaro.com/pro/pricing"
            class="flex items-center justify-center bg-primary hover:bg-charcoal hover:border-solid text-md text-white font-medium py-2 px-4 rounded-full w-auto md:w-[145px] h-[50px]"
          >
            Sign up
          </a>
        </div>

        <!-- Mobile menu button -->
        <div class="flex items-center lg:hidden">
          <button
            type="button"
            class="menu-button-mobile relative inline-flex items-center justify-center rounded-md p-2 text-primary hover:bg-gray-100 hover:text-gray-500 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-indigo-500"
            aria-controls="mobile-navigation-root"
            aria-expanded="false"
            aria-label="{{t 'toggle_navigation'}}"
            onclick="if (window.toggleMobileNavigation) { window.toggleMobileNavigation(); }"
          >
            {{#if signed_in}}
              {{user_avatar class="size-8 rounded-full mr-2 user-avatar"}}
            {{/if}}
            <svg
              class="size-6"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5"
              />
            </svg>
          </button>
        </div>
      </div>
    </div>
    <div id="radix-navigation-root" class="flex justify-center w-full"></div>
   


</header>
